<h1 id="page_title" data-mercury="simple"><%= @page.title.html_safe %></h1>
<div id="page_body" data-mercury="full">
  <%= @page.body.html_safe %>
</div>

<br />
<p>
  <%= link_to 'Edit', "/editor#{page_path(@page)}", class: 'btn btn-primary' %>
  <%= link_to 'Back', pages_path, class: 'btn btn-info' %>
</p>
